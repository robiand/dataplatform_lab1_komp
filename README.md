Simple python ETL pipeline script

Dirty data is loaded from csv file then cleaned and transformed by the script.
Various dataframes are created along the way, such as "rejected" or "valid" products based on several criteria.
Results are exported into csv files located in output folder.

Requires pandas
- uv pip install pandas

Instructions
- Run main.py